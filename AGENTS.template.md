# I. Состояние проекта

## 1. Сущность проекта

**<PROJECT_NAME>** — <краткое определение проекта>.

Проект ориентирован на:

- <цель 1>;
- <цель 2>;
- <цель 3>.

Основная ценность проекта — в согласованности между:

- <ключевая область 1>;
- <ключевая область 2>;
- <ключевая область 3>;
- кодом;
- тестами;
- документацией.

Проект не следует воспринимать как набор независимых скриптов. Если это не так, явно опишите допустимые исключения.

## 2. Топология проекта

Если в проекте есть `PROJECT_STRUCTURE.md`, каноническое описание файловой структуры находится там.

При обнаружении различий между фактическим состоянием файлов и содержимым `PROJECT_STRUCTURE.md` агент обязан сообщить пользователю и, если изменение очевидно и связано с текущей задачей, обновить `PROJECT_STRUCTURE.md`.

Рекомендуемая корневая структура:

```text
VERSION
CHANGELOG.md
CHANGELOG_ARCHIVE.md
AGENTS.md
PROJECT_STRUCTURE.md
README.md
pyproject.toml / package.json / Cargo.toml / ...

src/
    <package_or_app>/

docs/

tests/
research/
sandbox/
notes/
work/
```

Для проектов пользователя предпочтительна `src`-структура. Основной код пакетов и собираемых программ должен находиться в `src/`.

Если проект содержит несколько пакетов, каждый пакет должен размещаться внутри `src/`.

## 3. Назначение корневых каталогов

### `src/`

Основной код проекта.

Правила:

* основной код пакетов и собираемых программ размещается в `src/`;
* пакеты проекта не размещаются в корне репозитория;
* если проект содержит несколько пакетов, они размещаются как `src/<package_name>/`;
* временные скрипты, исследования и прототипы не размещаются в `src/`.

### `docs/`

Спецификации и сопровождающие документы проекта.

Документация является источником истины. Если агент обнаруживает отклонение между кодом и документацией, он обязан:

1. сообщить об этом пользователю;
2. либо обновить документацию в рамках текущей задачи;
3. либо явно указать, что документация требует отдельного обновления.

Ключевые документы проекта:

* `<docs/...>` — <назначение>;
* `<docs/...>` — <назначение>.

### `tests/`

Тесты и проверочные сценарии проекта.

Правила:

* структура тестов должна следовать существующей организации проекта;
* новые тесты должны добавляться рядом с родственными тестами;
* общая тестовая логика не должна копироваться хаотично;
* если проект запрещает импорт тестов друг из друга, общая логика выносится в `src/<package>/tools/` или аналогичный модуль;
* статусы тестов, тестовые карточки или списки тестов обновляются, если проект их использует.

### `research/`

Исследовательские сценарии.

Цель `research/` — получить характеристики, сравнения, численные результаты, экспериментальные данные или проверочные наблюдения, а не обязательно формальный pass/fail.

Правила:

* исследования должны быть воспроизводимыми;
* исследовательские сценарии не должны превращаться в скрытые зависимости основного кода;
* тяжёлые исследования запускает пользователь, если не указано обратное.

Если проект не использует `research/`, этот раздел можно удалить или отметить как резервный.

### `sandbox/`

Изолированная зона для экспериментов, прототипов, одноразовых скриптов и старого кода.

Правила:

* основной код не должен импортировать из `sandbox/`;
* тесты и исследования не должны зависеть от `sandbox/`;
* код из `sandbox/` не переносится в `src/` без рефакторинга;
* временные скрипты не создаются в корне проекта.

Если проект не использует `sandbox/`, этот раздел можно удалить или отметить как резервный.

### `notes/`

Исторические проектные решения пользователя.

`notes/` — это человеческая память проекта, а не временная зона агента.

Агент может читать `notes/` для понимания решений. Создавать или существенно менять заметки в `notes/` следует только если пользователь явно попросил сохранить решение именно как заметку.

Если проект не использует `notes/`, этот раздел можно удалить или отметить как резервный.

### `work/`

Рабочие документы взаимодействия пользователя и LLM.

`work/` используется для:

* планов разработки;
* анализа вариантов;
* промежуточных проектных решений;
* вопросов к пользователю;
* документов, созданных в одной LLM-сессии и исполняемых в другой;
* подготовки сложных изменений до редактирования кода.

Если задача нетривиальна, архитектурна, исследовательская или затрагивает несколько подсистем, агент по умолчанию сначала создаёт план в `work/`, а не сразу меняет код.

Формат имени:

```text
<version>_<date>_<title>.md
```

Где:

* `<version>` строится из первых двух чисел файла `VERSION`;
* если `VERSION == 0.8.12`, то `<version> == 0_8`;
* `<date>` записывается в формате `YYYY-MM-DD`;
* `<title>` — короткое имя латиницей или транслитерацией.

Пример:

```text
0_8_2026-05-10_feature_plan.md
```

Запрещено создавать промежуточные документы в корне проекта.

## 4. Источники истины

Агент обязан различать источники истины по областям.

| Область                               | Источник истины                                   |
| ------------------------------------- | ------------------------------------------------- |
| Структура проекта                     | `PROJECT_STRUCTURE.md`, фактическое дерево файлов |
| Общая идеология                       | `<docs/...>`                                      |
| Архитектура                           | `<docs/...>`                                      |
| Публичный API                         | `<src/...>`, README, API docs                     |
| Форматы данных                        | `<docs/...>`, тесты roundtrip                     |
| Тесты                                 | `<tests/...>`, test list/card docs                |
| История изменений                     | `CHANGELOG.md`, `CHANGELOG_ARCHIVE.md`, git log   |
| Пользовательские исторические решения | `notes/`                                          |

Если код и источник истины расходятся, агент не должен молча выбирать код как более актуальный.

Агент должен явно сообщить о расхождении и предложить одно из действий:

* привести код к документации;
* обновить документацию под фактическое поведение;
* зафиксировать вопрос в `work/` для решения пользователем.

## 5. Архитектурные инварианты

Агент обязан сохранять следующие инварианты:

* основной код находится в `src/`;
* пакеты проекта не размещаются в корне репозитория;
* публичные контракты не меняются без обновления документации и тестов;
* изменения форматов данных сопровождаются тестами совместимости;
* общая логика выносится в предназначенные для этого модули;
* `sandbox/` не является источником production-кода;
* `research/` не должен становиться скрытой зависимостью основного кода;
* изменения терминологии и архитектурных контрактов синхронизируются с соответствующей документацией.

Добавьте проектные инварианты:

* <инвариант 1>;
* <инвариант 2>;
* <инвариант 3>.

## 6. Антипаттерны

Запрещено:

* размещать основной код пакета вне `src/`;
* создавать временные скрипты или промежуточные планы в корне проекта;
* молча игнорировать расхождение между документацией и кодом;
* подменять архитектурное решение локальным хаком;
* смешивать исследовательский скрипт, функциональный тест и production-код в одном файле;
* менять `VERSION` без прямого разрешения пользователя;
* выполнять git-команды изменения без прямого разрешения пользователя;
* <проектный запрет 1>;
* <проектный запрет 2>.

---

# II. История проекта

## 7. VERSION

Файл `VERSION` содержит текущую версию проекта.

`VERSION` определяет:

* нумерацию новых блоков `CHANGELOG.md`;
* префиксы документов в `work/`;
* текущую линию разработки.

Агент не меняет и не инкрементирует `VERSION`.

Исключение возможно только при прямом, недвусмысленном указании пользователя.

Если агенту нужна версия для имени файла в `work/`, он читает `VERSION` и берёт первые два числа.

Пример:

```text
VERSION = 0.8.12
work prefix = 0_8
```

## 8. CHANGELOG.md

Все изменения агента должны быть отражены в `CHANGELOG.md`.

`CHANGELOG.md` — это буфер передачи ответственности от агента к пользователю и далее к `git+`.

Агент отвечает за содержательное описание изменения в блоке `[Unreleased]`.
Пользователь отвечает за ревью diff и закрытие изменения в git-историю.

### 8.1. Проверка `[Unreleased]`

Перед завершением задачи агент обязан проверить, есть ли в `CHANGELOG.md` блок с меткой `[Unreleased]`.

Если блок существует, агент добавляет запись в подходящую секцию этого блока.

Если блока нет, агент создаёт новый блок над последним существующим блоком.

### 8.2. Создание нового блока

При создании нового блока агент:

1. читает текущую версию из `VERSION`;
2. инкрементирует patch-часть только в заголовке changelog;
3. не меняет файл `VERSION`;
4. создаёт заголовок:

```markdown
## X.Y.Z [YYYY-MM-DD HH:MM:SS] [Unreleased]
```

Пример:

```markdown
## 0.8.13 [2026-05-10 18:30:00] [Unreleased]
```

### 8.3. Секции changelog

Рекомендуемые секции:

```markdown
### Added

### Changed

### Fixed

### Removed

### Docs

### Tests

### Internal
```

Допустимы русские секции, если текущий `CHANGELOG.md` уже ведётся на русском. Агент должен сохранять существующий стиль файла.

### 8.4. Требование к записи

Запись в `CHANGELOG.md` должна быть достаточно содержательной, чтобы пользователь понял смысл изменения без чтения полного diff.

При этом changelog не заменяет ревью diff.

Хорошая запись отвечает на вопросы:

* что изменено;
* зачем изменено;
* какие публичные или архитектурные контракты затронуты;
* какие тесты, docs или сценарии обновлены;
* какие проверки выполнены или должны быть выполнены пользователем.

## 9. CHANGELOG_ARCHIVE.md и релизная свёртка

При переходе проекта на новую крупную линию версий по прямому указанию пользователя агент:

1. читает текущий `CHANGELOG.md`;
2. выделяет важные изменения завершённой линии;
3. переносит подробное содержимое старого changelog в `CHANGELOG_ARCHIVE.md`, не дублируя уже перенесённые блоки;
4. заменяет рабочий `CHANGELOG.md` коротким релизным описанием новой версии;
5. не меняет `VERSION`, если пользователь не дал прямое разрешение;
6. фиксирует в релизном описании изменения правил, структуры, документации и архитектуры, если они были;
7. после релизной свёртки снова ведёт работу через новый `[Unreleased]` блок.

## 10. work-документы как основа диалога

Для сложных задач агент по умолчанию начинает с плана в `work/`.

Сложной считается задача, которая:

* создаёт новый контракт или меняет семантику существующего API;
* требует проектного выбора из нескольких альтернатив;
* меняет формат данных;
* меняет архитектуру;
* требует нетривиального выбора метода;
* требует серии тестов или исследований;
* может повлиять на обратную совместимость.

Не является сложной задача, которая затрагивает много файлов, но не требует проектного решения, например механическое переименование после согласованного решения.

### 10.1. План разработки

План в `work/` должен содержать:

```markdown
# <Название плана>

## Контекст

Что известно из задачи пользователя и текущего состояния проекта.

## Цель

Какой результат должен быть получен.

## Источники истины

Какие документы и файлы нужно учитывать.

## Предполагаемые изменения

Какие файлы или подсистемы могут быть затронуты.

## Вопросы к пользователю

Все неясные аспекты задачи.

## План работ

Пошаговая схема реализации.

## Проверки

Какие проверки выполнит агент, а какие должен выполнить пользователь.

## Риски

Что может сломаться или потребовать отдельного решения.
```

Во время планирования агент обязан задавать вопросы по неясным аспектам внутри документа плана.

Если пользователь одобрил или скорректировал план, агент исполняет уже согласованный план.

Если задача простая и локальная, агент может не создавать `work/`-план, но всё равно обязан обновить `CHANGELOG.md` после изменения.

## 11. Git-дисциплина

Git используется агентом только как инструмент наблюдения.

Разрешены read-only команды:

```bash
git status
git diff
git diff --staged
git log
git show
git blame
git rev-parse HEAD
git branch --show-current
```

Запрещены команды, изменяющие рабочее дерево, индекс, ветки, теги, историю или удалённые репозитории:

```bash
git add
git commit
git restore
git checkout
git switch
git reset
git revert
git merge
git rebase
git cherry-pick
git tag
git stash
git push
git pull
git fetch
```

Исключение возможно только по прямому, недвусмысленному приказу пользователя.

Даже если пользователь использует `git+`, агент не запускает `git+` самостоятельно, если пользователь явно не приказал это сделать.

## 12. GitPlus-контракт

Проект использует GitPlus-идеологию:

1. Агент готовит изменение.
2. Агент описывает изменение в `CHANGELOG.md` под `[Unreleased]`.
3. Пользователь смотрит diff в VSCode или другом инструменте.
4. Пользователь принимает, корректирует или отклоняет изменение.
5. Пользователь запускает команду `git+`.
6. `git+` обновляет `VERSION`, снимает `[Unreleased]` и создаёт унифицированный commit.

Агент не должен нарушать этот контракт.

---

# III. Правила разработки

## 13. Общий рабочий цикл агента

Перед изменением проекта агент должен:

1. прочитать `AGENTS.md`;
2. определить, какие источники истины относятся к задаче;
3. прочитать релевантные документы из `docs/`;
4. проверить текущее состояние файлов;
5. при необходимости посмотреть `git status` и `git diff`;
6. для сложных задач подготовить план в `work/`;
7. получить пользовательское одобрение плана, если задача требует проектного решения.

Во время изменения агент должен:

1. вносить минимальное связное изменение;
2. не смешивать независимые задачи без необходимости;
3. поддерживать синхронность кода, документации и тестовых спецификаций;
4. не разрушать существующие тестовые и исследовательские сценарии;
5. фиксировать обнаруженные расхождения между кодом и docs.

После изменения агент должен:

1. выполнить разрешённые проверки;
2. обновить `CHANGELOG.md`;
3. сообщить пользователю итог;
4. остановиться, не выполняя commit.

## 14. Минимальное связное изменение

Агент должен стремиться к минимальному связному изменению:

```text
одна цель → один связный набор правок → одна запись в CHANGELOG.md
```

Если задача распадается на несколько независимых изменений, агент должен:

* предложить разбиение;
* либо явно сгруппировать изменения в плане и changelog;
* не делать “заодно” архитектурно не связанные исправления без необходимости.

## 15. Правила импортов / зависимостей

<Опишите правила импортов, модульных зависимостей и доступов к внутренним частям проекта.>

Пример для Python-проекта:

```python
# Правильно
from <package> import PublicClass

# Неправильно для пользовательского кода
from <package>._internal import InternalClass
```

## 16. Правила тестов

Агент должен различать типы проверок проекта:

* быстрые unit-тесты;
* интеграционные тесты;
* функциональные сценарии;
* тяжёлые расчёты;
* визуальные проверки;
* исследовательские сценарии.

Агент может запускать только проверки, явно разрешённые проектом.

Опишите команды:

```bash
<typecheck command>
<unit test command>
<lint command>
```

Если проверку должен выполнять пользователь, агент готовит команду запуска и ожидаемый критерий результата, но не выполняет её самостоятельно.

## 17. Контроль качества

Для Python-проектов пользователя обычно предпочтительны:

```bash
python -m pyright src/<package>
python -m pytest tests/ -v
```

Для других стеков указать соответствующие команды:

```bash
<cargo test / npm test / go test / cmake --build / ctest / ...>
```

Все ошибки проверок должны быть исправлены перед завершением задачи, если они относятся к изменениям агента.

Если проверку невозможно выполнить из-за окружения, зависимостей, GPU, данных или длительности, агент обязан указать это в финальном отчёте.

## 18. Документация и синхронизация

Если изменение затрагивает контракт, терминологию, архитектуру, формат данных, публичный API, тестовый сценарий или математическую постановку, агент обязан обновить соответствующую документацию.

Правило:

```text
код изменил смысл → docs должны быть проверены
код изменил контракт → docs должны быть обновлены
код выявил расхождение с docs → пользователь должен быть уведомлён
```

Если агент не уверен, что документацию нужно менять, он должен явно указать это в финальном отчёте.

## 19. Финальный отчёт агента

После завершения задачи агент обязан сообщить пользователю итог.

Для тривиальных задач достаточно краткого итога и обновления `CHANGELOG.md`.

Для содержательных задач отчёт должен включать:

1. какие файлы изменены и что именно изменено;
2. какие источники истины были проверены;
3. обнаруженные расхождения между кодом и docs;
4. выполненные проверки и проверки, которые должен выполнить пользователь;
5. что добавлено в `CHANGELOG.md`;
6. риски или нерешённые вопросы.

Агент не должен предлагать выполнить commit самостоятельно.

---

# IV. Пайплайн изменений проекта для LLM

1. Руководствоваться `AGENTS.md`.
2. Найти релевантную область проекта:

   * `<area 1>`;
   * `<area 2>`;
   * `<area 3>`;
   * docs;
   * tests;
   * tools;
   * research;
   * sandbox.
3. Прочитать соответствующие документы-источники истины.
4. Проверить текущую структуру и существующие реализации.
5. Если задача сложная — создать план в `work/` и согласовать его с пользователем.
6. Если задача простая — выполнить минимальное изменение напрямую.
7. Обновить docs/tests/changelog в соответствии с изменением.
8. Выполнить только разрешённые проверки.
9. Дать пользователю итоговый отчёт.
10. Остановиться без commit.

---

# V. Краткая формула проекта

<PROJECT_NAME> — это <Суть системы>.

Агент должен поддерживать одновременно:

* корректность кода;
* согласованность с источниками истины;
* наблюдаемость изменений;
* воспроизводимость проверок;
* пользовательский контроль над историей проекта.
